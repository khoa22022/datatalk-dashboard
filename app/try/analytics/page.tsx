'use client';
import {Area,AreaChart,ResponsiveContainer,Tooltip,XAxis,YAxis,CartesianGrid,Bar,BarChart} from 'recharts';
import {CalendarDays,ChevronDown,Download,Search,Globe2,Smartphone} from 'lucide-react';
import {demoTraffic,demoPages,demoCountries} from '@/lib/demo-data';
export default function TrialAnalytics(){return <><header className="page-head"><div><div className="eyebrow">ANALYTICS</div><h1>Behavior overview</h1><p>Understand acquisition, engagement, and conversion without losing the UX context.</p></div><div className="head-actions"><button className="range"><CalendarDays size={16}/> Sep 9 – Sep 15 <ChevronDown size={15}/></button><button className="icon-btn"><Download size={17}/></button></div></header><div className="filter-row"><button>All pages <ChevronDown size={14}/></button><button>All devices <ChevronDown size={14}/></button><button>All countries <ChevronDown size={14}/></button></div><section className="content-grid wide-first"><div className="panel chart-panel"><div className="panel-head"><div><h2>Visitors</h2><p>Daily unique visitors</p></div></div><ResponsiveContainer width="100%" height={300}><AreaChart data={demoTraffic}><defs><linearGradient id="aFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#696cff" stopOpacity={.25}/><stop offset="100%" stopColor="#696cff" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="#eceef2"/><XAxis dataKey="day" axisLine={false} tickLine={false}/><YAxis axisLine={false} tickLine={false}/><Tooltip/><Area dataKey="visitors" type="monotone" stroke="#696cff" fill="url(#aFill)" strokeWidth={2}/></AreaChart></ResponsiveContainer></div><div className="panel"><div className="panel-head"><div><h2>Traffic by country</h2><p>Sessions by location</p></div><Globe2 size={18}/></div>{demoCountries.map(([country,users,share])=><div className="metric-line" key={country}><span>{country}</span><b>{users}</b><em>{share}</em></div>)}</div></section><section className="panel"><div className="panel-head"><div><h2>Top pages</h2><p>Compare engagement quality, not only traffic volume.</p></div><div className="search"><Search size={15}/><input placeholder="Search pages"/></div></div><div className="table"><div className="tr th"><span>Page</span><span>Views</span><span>Users</span><span>Avg. time</span><span>Exit</span></div>{demoPages.map(p=><div className="tr" key={p.path}><span className="strong">{p.path}</span><span>{p.views.toLocaleString()}</span><span>{p.users.toLocaleString()}</span><span>{p.avg}</span><span>{p.exit}</span></div>)}</div></section></>}
