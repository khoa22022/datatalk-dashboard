
"use client";
import {BarChart,Bar,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer} from "recharts";
const funnel=[{step:"Landing",users:10000},{step:"Product",users:6200},{step:"Contact",users:1116},{step:"Submit",users:268}];
export default function Funnels(){return <><div className="page-head"><div><h1>Funnels</h1><p>See where critical journeys lose momentum.</p></div><button className="btn primary">+ Create funnel</button></div><div className="grid grid-main"><div className="card"><div className="card-header"><div><h2 className="card-title">Checkout conversion</h2><p className="card-subtitle">10,000 users entered this journey.</p></div></div><div className="chart-wrap" style={{height:330}}><ResponsiveContainer><BarChart data={funnel} layout="vertical" margin={{left:15,right:20}}><CartesianGrid horizontal={false} stroke="#eef0f4"/><XAxis type="number" hide/><YAxis type="category" dataKey="step" axisLine={false} tickLine={false} width={70} tick={{fontSize:12,fill:"#596273"}}/><Tooltip/><Bar dataKey="users" fill="#696cff" radius={[0,7,7,0]} name="Users"/></BarChart></ResponsiveContainer></div></div><div className="card"><div className="card-header"><div><h2 className="card-title">Drop-off</h2><p className="card-subtitle">Conversion between steps.</p></div></div><div className="card-body">{funnel.map((x,i)=><div className="funnel-step" key={x.step}><b>{x.step}</b><div className="funnel-bar"><i style={{width:(x.users/100)+"%"}}/></div><span>{Math.round(x.users/100)}%</span></div>)}<div className="priority" style={{marginTop:20}}><span className="pill high">BOTTLENECK</span><h3>Product → Contact</h3><p>82% of users disappear before Contact. Investigate pricing attention and CTA visibility.</p></div></div></div></div></>}
