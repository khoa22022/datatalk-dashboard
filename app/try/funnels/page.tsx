'use client';
import {ChevronDown,Plus,ArrowDown,TrendingDown} from 'lucide-react';
import {demoFunnel} from '@/lib/demo-data';
export default function TrialFunnels(){return <><header className="page-head"><div><div className="eyebrow">FUNNELS</div><h1>Find the drop-off.</h1><p>See exactly where users stop moving toward the outcome.</p></div><button className="primary-btn"><Plus size={16}/> Create funnel</button></header><div className="panel funnel-panel"><div className="funnel-head"><div><h2>Contact conversion</h2><p>Last 7 days · 10,000 starting users</p></div><button className="mini-select">All devices <ChevronDown size={14}/></button></div><div className="funnel-list">{demoFunnel.map((s,i)=><div className="funnel-step" key={s.name}><div className="funnel-meta"><span>{i+1}. {s.name}</span><b>{s.users.toLocaleString()} users</b><em>{s.rate}%</em></div><div className="funnel-bar"><div style={{width:`${Math.max(s.rate,7)}%`}}/></div>{i<demoFunnel.length-1&&<div className="funnel-drop"><ArrowDown size={14}/><span>{Math.round((1-demoFunnel[i+1].users/s.users)*100)}% dropped</span></div>}</div>)}</div><div className="funnel-callout"><TrendingDown size={18}/><div><b>Largest bottleneck: Viewed product → Started contact</b><p>6,840 users viewed a product, but only 2,210 started contact. Consider a stronger CTA or shorter path.</p></div></div></div></>}
