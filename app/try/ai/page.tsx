'use client';
import {Sparkles,ArrowUpRight,CheckCircle2,AlertTriangle,ChevronDown} from 'lucide-react';
import {demoInsights} from '@/lib/demo-data';
export default function TrialAI(){return <><header className="page-head"><div><div className="eyebrow">AI ANALYST</div><h1>Turn behavior into decisions.</h1><p>Prioritized findings generated from the sample product behavior dataset.</p></div><button className="range"><Sparkles size={15}/> Analyze last 7 days <ChevronDown size={15}/></button></header><div className="ai-summary"><div><Sparkles size={20}/><b>3 actionable opportunities</b><span>1 high · 1 medium · 1 low</span></div><div><strong>86</strong><span>UX health score</span></div><div><strong>92%</strong><span>confidence</span></div></div><div className="ai-list">{demoInsights.map((i,idx)=><article className="ai-card" key={i.title}><div className="ai-index">0{idx+1}</div><div className="ai-body"><div className="ai-top"><span className={`severity ${i.severity.toLowerCase()}`}>{i.severity} priority</span><span>{i.confidence} confidence</span></div><h2>{i.title}</h2><div className="impact">{i.impact}</div><p>{i.text}</p><div className="recommend"><CheckCircle2 size={17}/><div><b>Recommended action</b><span>{i.action}</span></div></div></div><ArrowUpRight size={18}/></article>)}</div><div className="ai-disclaimer"><AlertTriangle size={16}/> Trial insights use sample data. Production AI will cite observed sessions, events and heatmaps.</div></>}
