'use client';
import { useState } from 'react';
import { AppShell } from '../../components/AppShell';
import { ArrowUpRight, Bot, Send, Sparkles } from 'lucide-react';
export default function AI(){ const [q,setQ]=useState(''); const [answer,setAnswer]=useState(''); function ask(){ if(!q) return; setAnswer('I found three signals: mobile conversion is 32% lower than desktop, 61% of users reach the form without interacting, and 214 repeated clicks occur around the primary CTA. The strongest hypothesis is form friction combined with weak action hierarchy.'); }
 return <AppShell title="AI Analyst"><div className="page-intro"><div><span className="eyebrow">INTELLIGENCE</span><h1>Ask Datatalk.</h1><p>Turn behavioral evidence into a decision you can act on.</p></div><span className="ai-status"><i></i> AI ready</span></div><div className="ai-layout"><div className="card ai-chat"><div className="chat-head"><div className="ai-symbol"><Sparkles size={17}/></div><div><strong>Datatalk AI</strong><span>UX & Product Intelligence</span></div></div><div className="chat-body"><div className="chat-intro"><Bot size={22}/><h3>What do you want to understand?</h3><p>Ask about conversion, friction, user behavior or a specific page.</p></div>{answer&&<div className="chat-answer"><span>Datatalk AI</span><p>{answer}</p><button className="text-button">View evidence <ArrowUpRight size={15}/></button></div>}<div className="suggestions"><button onClick={()=>setQ('Why is mobile conversion low?')}>Why is mobile conversion low?</button><button onClick={()=>setQ('Where are users getting stuck?')}>Where are users getting stuck?</button><button onClick={()=>setQ('What should I fix first?')}>What should I fix first?</button></div></div><div className="chat-input"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&ask()} placeholder="Ask Datatalk anything about this product..."/><button onClick={ask}><Send size={16}/></button></div></div><div className="card ai-side"><span className="eyebrow">TODAY'S SIGNALS</span><h3>3 things need attention</h3><div className="signal"><span className="severity high"></span><div><strong>Mobile conversion</strong><p>Down 32% vs desktop</p></div></div><div className="signal"><span className="severity medium"></span><div><strong>Repeated clicks</strong><p>214 around Contact CTA</p></div></div><div className="signal"><span className="severity low"></span><div><strong>Scroll depth</strong><p>64% average</p></div></div><button className="secondary-button full">Open UX audit</button></div></div></AppShell> }
