import { AppShell } from '../../components/AppShell';
import { Plus, ArrowDown, Sparkles } from 'lucide-react';
export default function Funnels(){ const steps=[['Landing','10,000','100%'],['View project','6,240','62.4%'],['Contact','1,280','12.8%'],['Submit','312','3.1%']]; return <AppShell title="Funnels"><div className="page-intro"><div><span className="eyebrow">CONVERSION</span><h1>Find the drop-off.</h1><p>Turn journeys into a clear path from intent to outcome.</p></div><button className="primary-button"><Plus size={16}/> New funnel</button></div><div className="card funnel-card"><div className="card-heading"><div><span className="eyebrow">BOOKING FUNNEL</span><h3>Portfolio → Contact</h3></div><span className="score-badge">3.1% converted</span></div><div className="funnel-steps">{steps.map((s,i)=><div className="funnel-step" key={s[0]}><div className="funnel-label"><span>{s[0]}</span><strong>{s[1]}</strong></div><div className="funnel-track"><div style={{width:s[2]}}></div></div>{i<steps.length-1&&<span className="funnel-drop"><ArrowDown size={13}/> drop</span>}</div>)}</div></div><div className="card funnel-ai"><div className="ai-symbol"><Sparkles size={17}/></div><div><span className="eyebrow accent-text">DATATALK AI</span><h3>The biggest leak is Project → Contact.</h3><p>79.5% of users who view a project never reach Contact. Mobile accounts for most of the gap.</p></div><button className="secondary-button">Explain this</button></div></AppShell> }
